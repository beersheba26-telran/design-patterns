# Decorator @cache
## Write decorator with the same behavior and functionality as the standard decorator @lru_cache(maxsize)
### Apply class LruCache from https://github.com/beersheba26-telran/python-intro/blob/lru-cache/lru_cache.py 